{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for local Lifebot commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
